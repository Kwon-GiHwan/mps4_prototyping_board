# Final figure and table audit

Manuscript: `paper-manuscript-submission-candidate`. Five figures, nine tables,
two appendices.

**Updated in the final polishing pass** for MOD-1 (figures renumbered by first
appearance) and MOD-2 (Figure 4's delta semantics). Table classifications are
unchanged from Phase 2.

## Figures (all `EXISTING_DATA_ONLY`)

Generated by `docs/paper/figures/make_figures.py`; provenance records in
`docs/paper/figures/FIGURE_PROVENANCE.json`. Deterministic SVG, no plotting
dependency, no manual calculation step.

| # | file | title | § | source artifact | columns | transformation | claim | forbidden reading |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `fig1_mac_scaling.svg` | Cumulative scaling efficiency per platform/NPU | 4.1 | `analysis/scaling.csv` | platform, npu, workload, mac, status, cumulative_efficiency | filter `status==EXECUTABLE`; plot the frozen column as-is, one panel per (platform, NPU) | scaling is workload-dependent and mostly sub-proportional | cross-panel comparison as cross-generation performance; panels share no absolute axis |
| 4 | `fig4_u85_group_delta.svg` | U85 256→512 per-group cycle change, rnnoise | 7.3 | `mechanism/U85_GROUP_DIFFERENTIAL.csv` | workload, binding_pair, n_ops, member_types, delta, direction | filter `rnnoise_INT8` + `B-frozen` (14 groups); sort by frozen delta | the reversal is distributed across groups; whole-model delta +19,000, profiled-group sum +19,060, residual 60 | any bar as *the cause*; per-operation attribution inside a merged interrupt window |
| 5 | `fig5_u85_memory_robustness.svg` | Cross-memory robustness of the regressing groups | 7.4 | `mechanism/U85_P1B_CROSSMODE_GROUPS.csv` | workload, n_ops, member_types, SO/SH/DS delta and dir | filter `rnnoise_INT8`; drop groups zero in all three modes; sort by frozen `DS_delta` | direction invariant across modes, magnitude not | bandwidth attribution; the modes are different artifacts |
| 3 | `fig3_board_relative_cost.svg` | FVP and board relative cost, independently normalized | 6 | `analysis/board_rq3/normalized_relative_cost.csv` | workload, fvp_normalized_cost, board_normalized_cost | sort by frozen FVP column; plot both frozen columns as-is | ordinal structure and cost shape transfer to the one hardware point | error, ratio, accuracy or deviation between the two bars; absolute comparison |
| 2 | `fig2_platform_sensitivity.svg` | Structural-metric agreement across platform change | 5 | `platform_sensitivity/X3_METRIC_QUALIFICATION.csv` | metric, class, tested_universe, agreement, disagreement, qualification | plot frozen counts per metric per class, drawn separately | ordinal metrics robust; thresholded class TA-state sensitive | pooling CLASS A with CLASS B; platform performance ranking |

Rows are listed in manuscript order of first appearance, which after the final
polishing pass is also numeric order. Figure 4 additionally reconciles the
whole-model observed delta (+19,000) against the reconstructed profiled-group
delta (+19,060) and names the 60-cycle deterministic profiling-boundary
residual, rather than presenting the group sum as the whole-model change.

No figure required a transformation the frozen analysis does not already define,
so no figure was stopped under the figure contract. The refusals in the last
column are enforced in the generator, not only in the caption: the script has no
code path that puts two platforms' raw cycles on one axis or computes an
FVP-versus-board difference.

## Tables

| # | § | table | classification | reason |
| --- | --- | --- | --- | --- |
| 1 | 3.1 | platform / NPU / TA / role | `KEEP_MAIN` | defines the experimental universe and the evidence limitation a reader needs before interpreting anything |
| 2 | 3.2 | workload Vela estimates | `KEEP_MAIN` | defines the workload universe; added in Phase 2 so the span claim carries its data (mi1) |
| 3 | 3.3 | the three kinds of number | `KEEP_MAIN` | defines measurement semantics; load-bearing for every comparison in the paper |
| 4 | 3.4 | evidence / compilation path / instrumentation | `KEEP_MAIN` | the compiler-path distinction is a limitation the reader must know before reading §7 |
| 5 | 4.1 | incremental efficiency class counts | `KEEP_MAIN` | load-bearing result; Figure 1 shows shape, not counts |
| — | 5 | CLASS A/B agreement counts | **`MERGE_MAIN`** | subsumed by table 6, which carries the same counts plus the qualification. Genuine redundancy |
| 6 | 5 | X3 metric qualification | `KEEP_MAIN` | the metric hierarchy the rest of the paper relies on (Mo5) |
| — | 6 | board rank pairs | **`MOVE_APPENDIX`** (A.1) | seven identical rank pairs; the result is stated as `rho = 1.0` / 0 inversions and shown in Figure 3. Exact pairs preserved for reproducibility |
| — | 6 | board normalized cost | **`MOVE_APPENDIX`** (A.2) | superseded visually by Figure 3; exact four-decimal values preserved for reproducibility |
| 7 | 7.4 | memory-mode whole-model deltas | `KEEP_MAIN` | load-bearing result; Figure 5 shows groups, this shows whole-model totals |

```
KEEP_MAIN          7
MERGE_MAIN         1
MOVE_APPENDIX      2
REMOVE_REDUNDANT   0
```

Nothing was removed. No table was dropped because its result was inconvenient.
Both moved tables retain their exact numbers in Appendix A, and all fourteen
appendix values are checked against the frozen CSV to four decimal places by
`phase2_validation.py`.

**One regression caught here.** The MERGE_MAIN edit deleted the invariant
sentence *"The two classes are reported separately and are not combined into a
single rate."* Consistency check 14 and the X1/X3 scope check both failed; the
sentence was restored rather than the checks relaxed.
